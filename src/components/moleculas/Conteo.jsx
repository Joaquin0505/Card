
export const Conteo = ({size}) => {
  return (
    <>
        <div className={`absolute bg-red-600 text-white text-[14px] rounded-full grid place-items-center -right-3 -top-1 ${size}`}>
            0
        </div>
    </>
  )
}
